# Vendored browser dependencies

- GridStack 13.0.2 (`gridstack-all.js`, `gridstack.min.css`) - MIT license
- Math.js 15.2.0 (`math.js`) - Apache-2.0 license

These pinned browser bundles power the draggable Sandbox layout and restricted formula calculations without requiring a CDN at runtime.
